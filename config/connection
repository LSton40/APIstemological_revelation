const { Sequelize } = require('sequelize');
require('dotenv').config();

const connection = new Sequelize('our_dbstep', 'root', process.env.DB_PASS,
  {
    host: 'localhost',
    dialect: 'mysql',
    logging: false
  }
);

module.exports = connection;